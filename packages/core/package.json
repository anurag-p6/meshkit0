{
  "name": "@ipfs-meshkit/core",
  "version": "1.0.0",
  "private": true,
  "description": "Core TypeScript SDK for IPFS Meshkit (bundled into root ipfs-meshkit).",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist"
  },
  "license": "MIT",
  "sideEffects": false,
  "dependencies": {
    "kubo-rpc-client": "^7.1.0"
  }
}
